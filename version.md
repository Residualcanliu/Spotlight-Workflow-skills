# Spotlight Workflow — 版本迭代记录

> 此文件不会被 Claude Code 作为技能加载。仅供人类阅读。

---

## v3.0 — 2026-09-20

**新增：写作铁律从三项扩至六项**

用户在 Graph RAG 项目协作中提出三条新写作偏好，全部是「AI 腔」问题：

- **铁律四：不用「一句话」类引导语** — 「一句话说明」「一句话结论」「一句话总结」「一句话概括」
  - 这四个字不携带信息，是 AI 腔最典型的标记。要总结就直接总结
- **铁律五：不用「说人话就是」类话术** — 「说人话就是……」「用人话说……」「通俗地讲就是……」
  - 用户原话：「我就是人啊，说的不就是我听得懂的吗」。这类前缀既多余，又暗示前半段"不是人话"
- **铁律六：不用「不是A，而是B」句式** — 改为先正面给出结论 B，再补一句"相比 A 好在哪"
  - 用户原话：「应该是采用B，相对于A来说好在哪」。否定式对比读起来像抬杠，正面陈述加理由信息量更大

同步修改：

| 文件 | 改动 |
|------|------|
| `SKILL.md` | §三 三项→六项铁律（含 [N]/[Y] 示例）；§七 速查 18→21 条并重排编号；复盘模板「一句话描述」→「简短描述」；版本号 |
| `REFERENCE.md` | §3 新增 3.2「六项铁律（完整示例）」，原 3.2/3.3/3.4 顺延为 3.3/3.4/3.5；清理 4 处「一句话」表述 |
| `spotlight-learn/SKILL.md` | 三项→六项；章节引用 §二→§三 修正；速查条数 15→21（原为陈旧值） |
| `spotlight-style/SKILL.md` | 三项→六项 |
| `spotlight-work/SKILL.md` | §二→§三 章节引用修正；三项→六项 |
| `README.md` | 内置铁律 3→6 条；「一句话概括」→「整体概括」；版本号 |
| `CLAUDE.md` | 当前状态版本号 |

---

## v2.9 — 2026-07-12

**新增：项目日志与存档点机制（§2.5）**

- 复杂项目启动时创建 `LOG.md`（项目日志），边做边记
  - 记录 TODO 进展、关键决策、方向转变
  - **运行输出文本化**：报错原文粘贴、结果文字描述，让非多模态 AI agent 也能通过读日志"看到"发生了什么
  - 日志是复盘的原材料——写复盘时翻 LOG.md 不遗漏细节
- 逻辑里程碑处设 git 存档点（`git commit -m "savepoint: ..."`）
  - 时机：完成 TODO 项后、高风险操作前、阶段性成果产出后、当日收工前
  - 回滚：`git log --grep="savepoint:"` 定位，`git reset --hard` 回滚
- 类比数据库：LOG.md = WAL，存档点 = 定期全量备份，协作 = PITR
- 触发标准复用 §2.4 复杂度判断（3+ 步骤或 2+ 文件）
- SKILL.md：§2.4 加触发规则一行、§七核心铁律从 17 条扩至 18 条（新增 #12）
- REFERENCE.md：新增 §2.5 完整说明（含与复盘的关系对比表）
- 更新 §八按需参考表

---

## v2.2 — 2026-06-26 / 更新 2026-06-27

**v2.2.0（06-26）新增：思维链拆解方法论（§1.0）**

- 从总体反推细节的拆解原则
- 用户导向的拆解深度控制（跟用户走，不过度拆）
- 五类任务 TODO 模板（Bug/开发/调研/文档/审查）
- TODO 驱动执行流程（主干→分支→叶子）
- 核心铁律新增 3 条思维铁律（#4-6），总数从 13 扩至 15
- 同步更新本地 CLAUDE.md 任务开始前章节

**v2.2.1（06-27）逻辑审查修复：17 个问题**

经由 skill-logic-reviewer 系统性审查全部 6 个 SKILL.md，修复：

*严重（2）：* gch-work §二 模式门冲突、gch-init mode 字段缺失
*高（8）：* 诊断模板同步、版本号统一、轻量模式升级提醒、learn_only 依赖警告、§六 mode 门、路径歧义消除等
*低（7）：* 铁律示例、{项目目录}解析规则、README learn-only、推荐模板/优先级、铁律计数 13→15 等

详见 `version.md` 底部 v2.2.1 完整清单及 `learn/2026年-06月-27日-GCH技能组逻辑审查.md`。

---

## v2.1 — 2026-06-26

**新增：写作风格双模式 + /gch-nogch 指令**

- `blend` 模式（默认）：GCH 内置风格 50% + 用户个人风格 50%
- `learn_only` 模式：100% 用户个人风格，GCH 内置规则不生效
- `/gch-nogch` 一键翻转开关
- `~/.gch_config.json` 新增 `writing_style_mode` 字段
- §二 写作风格手册新增模式说明
- 新增 gch-nogch/ 子命令技能

---

## v2.0 — 2026-06-26

**重大更新：可移植初始化 + 命令路由系统**

- 新增 §〇 首次运行初始化流程
  - 自动检测 `~/.gch_config.json`
  - 四个选项设置 learn 位置（当前目录/CLAUDE.md同根/自定义/AI决定）
  - 四个选项设置 claudework 位置
  - AI 决定模式含磁盘标红检测逻辑
  - 轻量模式（不建文件夹，仅启用思考逻辑）
- 所有硬编码路径替换为 `{learn_path}` 和 `{claudework_path}` 变量
- 新增命令路由系统：
  - `/gch` 自动模式
  - `/gch-init` 强制初始化
  - `/gch-work` 完整工作流
  - `/gch-check` 安装诊断
  - `/gch-learn` 仅思考模式
- 新增 §八 技能联动与推荐（任务完成后推荐未安装的 skill）
- 新增 §九 配置文件参考
- 新增 README.md 功能说明

---

## v1.0 — 2026-06-26

**初始版本**

基于三个复盘文件创建：

| 复盘文件 | 贡献内容 |
|---------|---------|
| 学习库搭建与用户思维初识 | 项目分级策略、迭代精炼思维、"即时应用"原则、Write 优于 Edit |
| 语义分割实验报告 | 五维风格框架、officecli 三步法、写作风格偏好（叙事感>教科书感）、两阶段审批工作流 |
| CNN手写数字识别大作业 | 三项铁律（禁用括号包参数/步骤不回顾/结果只一段）、batch JSON 引号陷阱、占位符标记法、图片保护策略 |

**初始结构：**
1. 项目全生命周期工作流
2. 用户写作风格手册
3. 用户思维与决策模式
4. 错误→正确路径清单
5. 工具使用最佳实践速查
6. 复盘与持续学习机制
7. 核心铁律速查（13条）
8. 技能联动

---

<br><br><br><br><br><br><br><br><br><br>

---

## 🥚 彩蛋

如果你读到了这里——
这是我的邮箱——

📧 **gaochaohongmail@foxmail.com**

爱你哦 ❤️

---
