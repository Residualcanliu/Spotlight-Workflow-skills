---
name: spotlight
description: >
  探照灯式个人工作流引擎。唯一入口 /spotlight，自动路由。
  核心：快速通道 + TODO驱动 + 写作铁律 + 复盘压缩。
  详细避坑清单、工具速查、思维模式见 REFERENCE.md。
---

# Spotlight — 探照灯式个人工作流引擎

> 个人工作方式操作系统。**这是核心指令文件（~10 KB），每次加载。**
> 写作规范见同目录下的 `WRITING.md`，详细参考手册见 `REFERENCE.md`，都按需读取。

---

## 〇、命令入口

**只有一个：`/spotlight`。** 系统自动判断意图并路由：

```
/spotlight [内容]
      │
      ├─ "初始化/安装/设置" → 进入初始化
      ├─ "检查/诊断/状态"   → 输出诊断报告
      ├─ "切换/写作风格"     → 翻转 blend ↔ learn_only
      ├─ 其他有实质内容      → 执行任务（快速通道判断 → 全流程或直接执行）
      └─ 无内容             → 检查安装状态，提示下一步
```

子命令（`/spotlight-init`、`/spotlight-work` 等）内部存在但不必记忆。

---

## 一、快速通道（最高优先级，先于一切）

**这是整个工作流的第一道闸门。** 在执行任何流程之前，先判断：

```
用户输入
    │
    ▼
触发条件（满足任一）：
  ① 输入 ≤ 20 字
  ② 仅涉及单文件修改（改变量名、修typo）
  ③ 纯问答（"这个函数干什么的？"）
  ④ 用户说"快点""直接改""不用走流程"
    │
    ▼
触发 → 跳过所有流程，直接执行。不回顾 learn、不拆解、不列 TODO、不写复盘。
       写作铁律和避坑清单静默生效（不宣告）。

未触发 → 走完整工作流（见下节）。
```

**反例（走全流程）：** "帮我写 CNN 实验报告"、"重构认证模块"、"分析这段代码的性能瓶颈"

> 核心原则：别在用户只想改个变量名的时候，输出一整篇回顾→拆解→TODO→复盘的念叨。

---

## 二、思维链与 TODO 驱动

> 如果触发了快速通道，跳过本节。

### 2.1 拆解原则：从总体反推细节

```
用户任务 → 从总体目标反推需要哪些模块 → 用户眼光照到哪就拆到哪
```

| 用户说了什么 | 拆解深度 |
|-------------|---------|
| 只给大致方向 | 拆第一层，不深入细节 |
| 追问某个模块 | 只深入那个模块，其他保持粗粒度 |
| "完整的都要拆" | 全链路逐层拆到底 |
| "先不管XX" | 跳过该模块，标记为 [待定] |

**核心：拆解跟着用户的注意力走。用户看哪里拆哪里，不提前过度拆解。**

### 2.2 拆完必列 TODO（默认 3 条，复杂可 4+）

| 任务类型 | TODO 模板 |
|---------|----------|
| Bug 排查 | ①定位问题 ②分析链路 ③修复+验证 |
| 功能开发 | ①理清需求边界 ②设计实现路径 ③确定验收标准 |
| 调研选型 | ①明确调研目标 ②收集对比方案 ③输出结论建议 |
| 文档写作 | ①确定结构范围 ②填充核心内容 ③检查精简 |
| 代码审查 | ①明确审查维度 ②逐项检查记录 ③汇总优先级 |

**复杂任务可 4+：** 多独立子系统、并行工作流、额外验证阶段均可增加。
**反例：** 一个 Bug 硬拆成 5 步、把"设计实现路径"拆成"选技术栈"+"写代码"——这些是子步骤，不是独立 TODO。

### 2.3 执行：主干 → 分支 → 叶子

```
TODO (主干)
  └── 子步骤 (分支)
        └── 具体操作 (叶子)
```

按 TODO 顺序逐条执行，复杂 TODO 内部再拆 3 个子步骤。

### 2.4 任务复杂度判断

| 复杂度 | 标准 | 策略 |
|--------|------|------|
| 复杂 | 3+ 独立步骤 或 2+ 文件 | TODO → 三层拆 |
| 简单 | 单文件、线性步骤 | 不建 TODO，自顶向下直接拆 |

**复杂任务额外规则：** 创建 `LOG.md` 项目日志（边做边记，运行输出文本化，供非多模态 agent 读取），里程碑处设 git 存档点。详见 REFERENCE.md §2.5。

---

## 三、写作十一项铁律

> `writing_style_mode = "blend"`（默认）时生效。切到 `learn_only` 后完全以 learn 中的个人风格为准。
>
> **完整示例、判断依据、真实改稿案例见同目录下的 `WRITING.md`。** 这里只留速查。

**词汇与句式**

1. 不用括号包参数 — 「Adam，学习率0.0008」而非「Adam(lr=0.0008)」
2. 步骤不写回顾 — 步骤是「做了什么」，不是「学到了什么」
3. 结果只写一段 — 汇总结论，不逐场景展开
4. 不用「一句话」引导语 — 要总结就直接总结
5. 不用「说人话就是」类话术 — 读者本来就是人
6. 不用「不是A，而是B」— 先给结论 B，再说相比 A 好在哪

**结构与语气**

7. 标题用名词短语 — 「查询流程」而非「查询是怎么走的」
8. 不写元话语 — 不预告、不设悬念、不自指、不导览
9. 破折号只断句，不造金句 — 中文技术写作里它出现频率很低
10. 先声明数量再编号 — 「有两种，如下：1. 2.」
11. 动词用书面词，只写正向结论 — 「采用/检测/确认」而非「用/抓/碰」；不写假设性反面论证

**写完自检三问**

1. 这句删掉，读者会少知道一个事实吗？不会 → 删
2. 这个标题在陈述东西，还是在跟读者说话？后者 → 改名词短语
3. 这里的破折号在断句，还是在憋金句？后者 → 改普通句

> AI 腔的根源：文字里有大量**关于这段文字本身的内容**，而不是关于事实的内容。

---

## 四、指令风格速查

| 用户说什么 | 实际含义 |
|-----------|---------|
| "缩短点，讲清楚原理即可" | AI 自行判断砍多少 |
| "ok" | 确认通过，继续下一个 |
| "里面图片不动" | 图片位置/编号/尺寸已审核，不要碰 |
| "结果可以写为[结果参数]" | 用占位符，先搭框架后填数据 |
| "再往下点、稍微放大、透明度稍微加大" | 模糊描述 → AI 自主推断具体数值，一次调 2-3 维 |
| "慢慢优化下" | 小步迭代，每次只做一步，等反馈 |

---

## 五、初始化（首次使用）

**默认策略：在当前工作目录创建。** 不需要选择题。

```
首次调用 /spotlight
    │
    ▼
检测 ~/.spotlight_config.json
    │
    ├─ 已存在 → 加载配置，继续
    │
    └─ 不存在 → "Spotlight 将在当前目录创建 learn/ 和 claudework/ 两个文件夹。
                 可以吗？"
                ├─ 可以 → 创建，写入配置，完成
                ├─ 自定义 → 询问 learn 路径 → 询问 claudework 路径 → 创建
                └─ 跳过  → 进入轻量模式（不创建文件夹，仅启用思考逻辑）
```

**轻量模式：** 不创建文件夹，不写复盘。每完成 5 个重要任务后询问是否升级。

**配置文件：** `~/.spotlight_config.json`

```json
{
  "mode": "full",
  "learn_path": "D:\\learn",
  "claudework_path": "D:\\claudework\\work",
  "writing_style_mode": "blend",
  "initialized": true,
  "version": "2.7"
}
```

---

## 六、复盘机制

### 何时写
项目完成、重要里程碑、值得记录的错误、用户要求、发现新思维模式。

### 怎么写
文件路径：`{learn_path}\YYYY年-MM月-DD日-项目名称.md`

```
# 项目名称 — 简短描述
## 项目概述（1-2行）
## 错误 → 修复（症状 → 修复方法）
## 优化点（原来方式 → 改进方式）
## 用户方法论观察（思维模式、决策偏好等新发现）
## 应保留的模式
## 核心经验教训（2-5条可行动规则）
```

### 防臃肿规则

| 规则 | 说明 |
|------|------|
| 每条教训 ≤ 50 字 | 够说清"症状→修复"。[N] "在处理 officecli batch JSON 时中文引号导致解析失败" → [Y] "batch JSON 中文引号导致解析失败，用方括号替代" |
| >20 条自动压缩 | 合并为 ≤5 条核心原则，原文附为摘要 |
| 只读近 3 篇摘要 | 任务前只读最近 3 篇的"核心经验教训"，不加载全库 |
| >30 个文件归档 | 最旧 10 个移入 archive/，生成一句摘要索引 |

---

## 七、核心铁律速查（26 条）

### 写作（一~十一的压缩版，完整见 WRITING.md）
1. 不用括号包参数 — "Adam，学习率0.0008" 而非 "Adam(lr=0.0008)"
2. 步骤不写回顾 — 步骤写完就停
3. 结果只写一段 — 汇总结论，不逐场景展开
4. 禁用 Emoji — 功能性 Unicode（← → ×）可保留
5. 不用「一句话」引导语 — 要总结就直接总结
6. 不用「说人话就是」类话术 — 读者本来就是人
7. 「不是A而是B」改正面陈述 — 先给结论 B，再说相对 A 好在哪
8. 标题用名词短语 — 「查询流程」而非「查询是怎么走的」
9. 不写元话语 — 不预告、不设悬念、不自指、不导览
10. 破折号只断句不造金句 — 中文技术写作里它出现频率很低
11. 先声明数量再编号 — 「有两种，如下：1. 2.」
12. 动词用书面词，只写正向结论 — 不写假设性反面论证

### 思维
13. 从总体反推细节 — 先看整体目标，逐层往下拆
14. 拆解深度跟用户走 — 用户看哪里拆哪里
15. 分析完必列 TODO — 默认 3 条，复杂可 4+；4+ 必须有明确理由

### 工作流
16. 快速通道优先 — ≤20 字或单文件 → 跳过全流程
17. 做完初版主动呈现，留迭代空间
18. 新规则立完立刻用，形成闭环
19. 事毕必复盘
20. 复杂项目先建日志，里程碑 git 存档 — 过程可追溯、出错可回滚

### 工具
21. officecli batch JSON 禁止 ASCII 双引号 — 用方括号替代
22. 编辑前必跑 `officecli query image`
23. 用 stable paraId 寻址，不用位置索引
24. 中英混合修改用 Write 全量覆写，不用 Edit

### 第三方框架
25. 先查文档再写代码 — WebSearch 找官方示例的 import/API 签名，不凭其他框架经验猜测

### 学习
26. 事毕必复盘，每次任务前只读近 3 篇摘要

---

## 八、按需参考

**以下内容不在本文件中（防臃肿），需要时读取同目录下的文件：**

| 需要什么 | 读哪里 |
|---------|--------|
| **写作铁律的完整示例、判断依据、真实改稿案例** | **`WRITING.md`（整份）** |
| 新项目创建流程 | `REFERENCE.md` §1 项目启动 |
| 迭代精炼、两阶段审批、文档编辑工作流、项目日志与存档点 | `REFERENCE.md` §2 工作流模式 |
| 五维风格框架、图片桥接规范 | `REFERENCE.md` §3 写作风格 |
| 用户思维模式完整描述 | `REFERENCE.md` §4 思维模式 |
| 完整避坑清单（officecli / 文件 / Python / React） | `REFERENCE.md` §5 避坑清单 |
| 工具命令速查（officecli / sed / 3D） | `REFERENCE.md` §6 工具速查 |
| 技能联动与推荐 | `REFERENCE.md` §7 技能联动 |
| 配置文件完整参考 | `REFERENCE.md` §8 配置参考 |

> **读取方式：** 遇到相关场景时，用 Read 工具读取对应文件的对应章节。
> 不要一次加载全部。改稿或写文档时，读 `WRITING.md`。

---

> **版本：** v3.1 — 写作铁律从六项扩至十一项（新增结构层五条：名词短语标题、不写元话语、破折号不造金句、先声明数量、书面动词与正向结论）；完整规范拆到 `WRITING.md`
> **最后更新：** 2026-09-20
