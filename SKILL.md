---
name: spotlight
description: >
  探照灯式个人工作流引擎。
  子命令：/spotlight(自动) /spotlight-init(初始化) /spotlight-work(工作流) /spotlight-check(检查) /spotlight-learn(仅思考)。
  包含：项目创建与Init流程、写作风格三项铁律、用户思维决策模式、
  officecli工具避坑指南、复盘学习机制、以及从历次项目中积累的错误→正确路径。
  首次使用时会引导用户完成初始化设置（learn复盘库 + claudework工作区），支持轻量模式。
---

# Spotlight — 探照灯式个人工作流引擎

> 这不是一个通用技能。这是一个为你量身定制的工作方式操作系统，融合了你的工作流设计、写作风格、思维模式、以及我在历次项目中犯过的错误和找到的正确方法。

---

## 命令参考

> **用户只需要记住一个命令：`/spotlight`。** 其他子命令是内部路由实现，不需要记忆。

### `/spotlight [内容]` — 唯一入口

输入 `/spotlight` 后，系统自动判断你的意图并路由到对应子流程：

```
/spotlight [内容]
      │
      ├─ "初始化" / "安装" / "设置" / "配置" 关键词
      │   → 自动路由到初始化流程
      │
      ├─ "检查" / "诊断" / "状态" 关键词
      │   → 自动路由到诊断报告
      │
      ├─ "切换" / "写作风格" / "纯学习" 关键词
      │   → 自动路由到风格切换
      │
      ├─ 其他有实质内容的输入
      │   → 执行任务（快速通道判断 → 全流程或直接执行）
      │
      └─ 无内容（只输入了 /spotlight）
          → 检查安装状态 → 提示下一步
```

**执行顺序（有任务内容时）：**

```
用户输入 /spotlight [附带内容]
       │
       ▼
┌─ 快速通道判断（§1.0.0）─────────────────────────┐
│  ≤ 20 字 或 仅涉及单文件修改？                    │
│  → 跳过所有流程，直接执行，不建 TODO，不写复盘      │
│                                                   │
│  多文件 / 多步骤 / 新项目？                        │
│  → 走完整工作流：回顾 learn → 拆解 → TODO → 执行    │
└───────────────────────────────────────────────────┘
       │
       ▼
  检查 ~/.spotlight_config.json
       │
   ┌───┴───┐
   │ 已安装 │ → 加载配置 → 按完整工作流执行
   └───────┘
   │ 未安装 │ → "Spotlight 工作流引擎尚未初始化。需要现在安装吗？"
   └───────┘
         │
     ┌───┴───┐
     │ 是     │ → 进入安装流程（§0.3-0.4）
     └───────┘
     │ 否     │ → 轻量模式（§0.5）
     └───────┘
```

### 子命令（内部路由，可直调但非必须）

以下命令在 `/spotlight` 内部自动路由，你也可以直接调用：

| 命令 | 等价于 |
|------|--------|
| `/spotlight-init` | `/spotlight 初始化` |
| `/spotlight-work` | `/spotlight` + 完整工作流模式 |
| `/spotlight-check` | `/spotlight 检查状态` |
| `/spotlight-learn` | `/spotlight` + 轻量模式 |
| `/spotlight-style` | `/spotlight 切换写作风格` |

---

## 〇、首次运行：初始化设置

> **初始化逻辑由命令路由层（上节）触发。本节定义具体的安装流程。**

### 0.1 检测是否已初始化

检查配置文件 `~/.spotlight_config.json` 是否存在：

- **已存在** → 读取配置，跳至 0.6 加载路径，然后进入第一章。
- **不存在** → 继续 0.2，执行首次初始化。

### 0.2 欢迎与说明

向用户展示以下信息：

```
══════════════════════════════════════════
  Spotlight 工作流引擎 — 首次初始化
══════════════════════════════════════════

本技能将为你的工作流建立两套统一的文件夹体系：

learn（复盘库）
   用途：每个项目完成后的经验教训、错误→修复记录、
        用户思维模式观察、写作风格提炼。
   内容：以日期命名的 .md 复盘文件。

claudework（项目工作区）
   用途：所有项目的集中存放目录。每个新项目按
        "YYYY年-MM月-DD日-项目名称" 格式在此创建。
   逻辑：新项目统一从这里创建 → Init 生成 CLAUDE.md →
        开发 → 复盘写入 learn。

══════════════════════════════════════════
```

然后询问用户：

> **是否进行初始化设置？**（创建以上两套文件夹体系）
> - **是** → 依次询问 learn 和 claudework 的存放位置
> - **否** → 进入轻量模式（仅启用思考逻辑和历史复盘参考，不创建文件夹）

如果用户选**否**，跳至 0.5 轻量模式。

### 0.3 设置 learn（复盘库）位置

向用户展示选项：

> **请选择 learn（复盘库）文件夹的存放位置：**

| 选项 | 说明 |
|------|------|
| **1. 当前文件夹** | 在你当前打开命令行/IDE 的目录下创建 `learn/` |
| **2. CLAUDE.md 同根目录** | 找到你电脑中的全局 CLAUDE.md（`~/.claude/CLAUDE.md`），在其同级目录下创建 `learn/` |
| **3. 自定义路径** | 你自己输入一个绝对路径。如果路径不存在，会再次请你确认 |
| **4. 由 AI 决定** | AI 分析你的磁盘情况后自动选择最佳位置 |

**各选项处理逻辑：**

**选项 1 — 当前文件夹：**
- 在当前工作目录（`pwd`）下创建 `learn/`
- 路径写入配置

**选项 2 — CLAUDE.md 同根目录：**
- 先查找 `~/.claude/CLAUDE.md` 是否存在
- 如果存在 → 在 `~/.claude/learn/` 创建 `learn/`（统一使用 `~/.claude/learn/`，保持与 Claude 配置的关联。注意：此路径在 `.claude/` 目录内部，而非其同级目录）
- 如果不存在 → 告知用户未找到全局 CLAUDE.md，建议改用选项 4

**选项 3 — 自定义路径：**
- 请用户输入绝对路径
- 检查路径是否存在
- 如果不存在 → 告知用户并询问是否创建该目录，如用户不同意则重新输入
- 如果存在且可写 → 在该路径下创建 `learn/` 子目录

**选项 4 — 由 AI 决定：**
执行以下分析逻辑：

```
1. 检测操作系统和磁盘情况：
   - Windows: 用 powershell 或 wmic 获取各盘符的剩余空间
   - Linux/Mac: 用 df -h 获取挂载点空间
2. 判断标准（"标红" = 剩余空间 < 总空间的 10% 或 < 1GB）：
   - 系统盘（Windows C: 或 Linux / ）是否标红？
   - 是否有其他可用盘符？
3. 决策：
   - 如果没有盘标红 → 在 CLAUDE.md 同根目录创建（`~/.claude/learn/`）
   - 如果有盘标红 且 存在 D 盘 → 在 D 盘根目录创建 `D:\learn\`，
     并在文件夹中额外放置一个 `README.md` 说明这是 Claude 的复盘文件夹
   - 如果有盘标红 但 只有系统盘 → 仍在 CLAUDE.md 同根目录创建，
     但提醒用户磁盘空间紧张
```

### 0.4 设置 claudework（项目工作区）位置

向用户展示选项：

> **请选择 claudework（项目工作区）文件夹的存放位置：**
>
> claudework 是你所有项目的集中存放目录。以后每次创建新项目，都会在这个目录下按 `YYYY年-MM月-DD日-项目名称` 的格式生成项目文件夹。

| 选项 | 说明 |
|------|------|
| **1. 当前文件夹** | 在你当前打开命令行/IDE 的目录下创建 `claudework/` |
| **2. CLAUDE.md 同根目录** | 在全局 CLAUDE.md 同级目录下创建 `claudework/` |
| **3. 自定义路径** | 你自己输入一个绝对路径 |
| **4. 由 AI 决定** | AI 分析你的磁盘情况后自动选择最佳位置 |

**各选项处理逻辑与 0.3 相同**，但以下调整适用：
- 创建的目录名是 `claudework/`（而非 `learn/`）
- **0.3 中提到的 README.md 放置仅适用于 learn 目录**，claudework 目录不需要放置 README.md
- 选项 4 磁盘判断逻辑相同，但目标目录为 claudework

### 0.5 轻量模式

用户选择不初始化时：

- 保存配置：`{ "mode": "lightweight", "initialized": true }`
- **仍然启用：**
  - 写作风格三项铁律
  - 用户思维与决策模式（第三章）
  - 错误→正确路径避坑（第四章）
  - 工具使用最佳实践（第五章）
  - 核心铁律速查（第七章）
- **不启用：**
  - 项目创建工作流（1.1）
  - 复盘写入机制（第六章）
  - learn 目录相关操作
- 后续用户可以随时说"初始化 Spotlight 工作流"来重新进入完整初始化流程
- **定期提醒：** 每完成 5 个重要任务后，如果仍在轻量模式，主动询问一次是否要升级为完整模式（"您已使用轻量模式完成多个任务。是否需要现在初始化完整工作流？"）。避免用户长期使用轻量模式却不知道自己错过了完整功能

### 0.6 加载已保存的配置

初始化完成后（或后续每次调用时），从 `~/.spotlight_config.json` 读取配置，加载以下变量供后续章节使用：

- `{learn_path}` — 复盘库根目录（如 `D:\learn\`），轻量模式下为空
- `{claudework_path}` — 项目工作区根目录（如 `D:\claudework\`），轻量模式下为空
- `{mode}` — `"full"` 或 `"lightweight"`

**配置文件示例：**
```json
{
  "mode": "full",
  "learn_path": "D:\\learn",
  "claudework_path": "D:\\claudework\\work",
  "initialized": true,
  "version": "2.6"
}
```

---

## 一、项目全生命周期工作流

> 如果 `{mode}` = `"lightweight"`，跳过 1.1（项目创建工作流），直接执行 1.2-1.7。

### 1.0.0 快速通道：场景判断（最高优先级，先于一切）

**这是整个工作流的第一道闸门。** 在拆解任务、回顾 learn、列出 TODO 之前，先判断：这个任务值得走全流程吗？

```
用户输入
    │
    ▼
┌─────────────────────────────────────────────┐
│  快速通道条件（满足任一即触发）：              │
│                                              │
│  ① 用户输入 ≤ 20 字                          │
│  ② 仅涉及单文件修改（改个变量名、修个typo）    │
│  ③ 纯问答（"这个函数是干什么的？"）           │
│  ④ 用户明确说"快点""直接改""不用走流程"       │
└─────────────────────────────────────────────┘
    │
    ▼
触发快速通道 → 跳过以下所有步骤：
  [N] 不回顾 learn
  [N] 不拆解任务
  [N] 不列 TODO
  [N] 不写复盘
  直接执行用户请求
  仍启用写作铁律和避坑清单（静默生效，不宣告）
```

**反例（不触发快速通道）：**
- "帮我写一份 CNN 实验报告" → 多段落、项目级任务 → 走全流程
- "重构认证模块" → 多文件、有依赖 → 走全流程
- "分析这段代码的性能瓶颈" → 需要拆解 → 走全流程

> **核心原则：别在用户只想改个变量名的时候，给他输出一整篇回顾→拆解→TODO→复盘的念叨。**

### 1.0 思维链：任务拆解与 TODO 驱动

> 如果触发了快速通道（§1.0.0），跳过本节。直接执行任务。

这是所有任务的第一步——拿到任务后，先拆解，再列 TODO，然后才动手。

#### 1.0.1 拆解原则：从总体反推细节（Top-Down Reverse Decomposition）

```
用户任务
    │
    ▼
从总体目标出发 → 反推需要哪些模块 → 每个模块需要哪些细节 → ...
    │
    ▼
只拆用户导向的那部分，不过度拆解
```

**示例：用户说"做眼镜的手势识别"**

```
眼镜手势识别
├── 硬件层：眼镜上需要摄像头 + 计算模块
│   ├── 摄像头选型（小型化、低功耗）
│   ├── 计算模块（边缘计算芯片）
│   └── ...（用户没问就不继续拆）
├── 算法层：手势识别模型
│   ├── 需要轻量化模型 → 有哪些轻量化的？
│   │   ├── MobileNet 系列
│   │   ├── ShuffleNet
│   │   └── ...（用户引导到哪就拆到哪）
│   └── 训练数据需求
└── 系统层：摄像头采集 → 模型推理 → 手势输出
```

#### 1.0.2 拆解深度：用户导向原则

| 用户说了什么 | 拆解深度 |
|-------------|---------|
| 只给了大致方向（"做个手势识别"） | 拆第一层（硬件/算法/系统），不深入细节 |
| 追问了某个模块（"轻量化模型有哪些"） | 只深入那个模块，其他模块保持粗粒度 |
| 明确说"完整的都要拆"、"全部分析" | 全链路剖析，所有模块逐层拆到底 |
| 说了"先不管XX"、"XX后面再说" | 跳过被标记的模块，标记为 [待定] |

**核心：拆解跟着用户的注意力走。** 用户看向哪里就拆哪里，没说的地方保持粗粒度。不要提前拆解用户没问的部分。

#### 1.0.3 拆解完成后：默认列出 3 条 TODO（复杂任务可 4+）

任何任务分析完后，将拆解结果浓缩为 TODO 列表。**大部分情况 3 条即可**，按下方模板选用。如果任务涉及多个独立子系统、跨多个技术栈、或有明确的并行工作流，可以列出 4 条或更多——但要有明确理由，不为了凑数而拆分。

不同任务类型有不同的 TODO 模板（以下为默认 3 条结构）：

**Bug 排查类：**
```
1. 定位相关问题（复现条件、涉及模块、关键日志）
2. 分析整个链路（从触发点到出错点的完整路径）
3. 形成修复方法 + 验证方法
```

**功能开发类：**
```
1. 理清需求边界（做什么、不做什么、输入输出）
2. 设计实现路径（技术选型、模块划分、关键接口）
3. 确定验收标准（怎么算做完、怎么验证正确）
```

**调研/选型类：**
```
1. 明确调研目标（需要回答什么问题）
2. 收集和对比候选方案（各方案的优劣表）
3. 输出结论和推荐（选哪个、为什么、风险在哪）
```

**文档/写作类：**
```
1. 确定结构与范围（章节划分、每章覆盖什么）
2. 填充核心内容（按骨架逐章写入）
3. 检查与精简（删冗余、统术语、校错误）
```

**代码审查类：**
```
1. 明确审查维度（正确性/性能/安全/可维护性）
2. 逐项检查并记录问题（位置 + 问题 + 建议）
3. 汇总问题等级和修复优先级
```

**何时扩展到 4 条或更多？**

满足以下任一条件时，可以在默认 3 条基础上增加：

| 触发条件 | 示例 |
|---------|------|
| 涉及多个独立子系统，无法合并 | "重构认证模块 + 数据库迁移 + 前端适配" → 3 个子系统各自独立 |
| 有明确的并行工作流 | "后端 API + 前端页面 + 移动端适配" 可并行开发 |
| 需要额外的验证/测试阶段 | 安全关键项目 → 增加"安全审查"作为第 4 条 |
| 用户明确要求列出更多细节 | 用户说"拆详细点"、"全部列出来" |

**反例（不应拆成 4+ 的情况）：**
- 一个 Bug 的定位和修复硬拆成 4 步（定位 → 分析 → 修复 → 验证 → 再加一个"写测试"）——"验证"已包含测试
- 为了凑数量把"设计实现路径"拆成"选技术栈"+"写代码"——这是一条 TODO 的两个子步骤

> **核心原则：3 条是默认，4+ 要有明确的"为什么这第 4 条不能合并到前 3 条里"。**

#### 1.0.4 TODO 驱动执行

```
分析完成 → N 条 TODO（默认 3，复杂可 4+）
                │
                ▼
        按 TODO 顺序逐条执行
                │
        ┌───────┴───────┐
        │ 复杂 TODO      │ → 每条 TODO 内部再拆 3 个子步骤
        │                │   → 主干(TODO) → 分支(子步骤) → 叶子(具体操作)
        └───────────────┘
                │
                ▼
        每条 TODO 完成后标记 [Y]
                │
                ▼
        全部完成后 → 复盘 → learn
```

### 1.1 新项目启动流程 (Project Bootstrap)

```
用户提出需求 → 创建项目目录 → cd 进入 → 搭建脚手架 → 运行 Init → 开始开发
```

**具体步骤：**

1. **创建目录**：`{claudework_path}\YYYY年-MM月-DD日-项目名称`
   - 日期格式：`YYYY年-MM月-DD日`（如 `2026年-06月-26日-my-project`）
   - 如果用户未指定项目名称，先询问
2. **切换工作目录**到新项目文件夹
3. **搭建项目脚手架**（按项目类型）
4. **运行 `/init`**（Init skill）：在脚手架就位后、深入开发之前生成 CLAUDE.md
5. **开始开发**

### 1.2 任务复杂度判断与执行策略

这是每个任务开始前的第一步判断：

| 复杂度 | 判断标准 | 执行策略 |
|--------|---------|---------|
| **复杂项目** | 多文件、多步骤、有依赖关系 | 先定关键TODO（默认3条，复杂可4+） → 逐条细分 → 三层结构：主干→分支→叶子 |
| **简单项目** | 单文件、线性步骤、无依赖 | 不建TODO，自顶向下，从整体到细节一步步拆解 |

**关键判断点：** 如果任务涉及3个以上独立步骤或2个以上文件，按复杂项目处理。

### 1.3 迭代精炼工作流 (Iterative Refinement)

用户不会一次说完所有需求。这是正常的，不是遗漏：

```
用户给核心要求 → AI执行初版 → 呈现给用户 → 用户补充/修正 → AI调整 → 循环至完成
```

**执行要点：**
- 做完初版后**主动呈现**，不要等用户来问
- 用户的补充约束（如"XX不动"）要**立刻确认并执行**
- 如果已在编辑的内容涉及约束区域，需要回退或跳过

### 1.4 两阶段审批工作流 (Two-Phase Approval)

适用于：风格模仿、大段改写、设计方案等容易有理解偏差的任务。

```
Phase 1: 总结规律/方案 → 用户判断方向 → Phase 2: 执行 → 用户确认结果
```

**核心原则：** 向用户展示中间检查点（如风格总结、设计方案概要），能大幅降低返工风险。

### 1.5 文档编辑工作流 (Document Editing)

**officecli 三步法（标准操作）：**

```
Step 1: get --json 读结构 → Step 2: set/remove/add 做修改 → Step 3: get --depth 1 快速验证
```

**多段落批量修改流程：**
1. `officecli get` 读全貌（用 `--depth 3` 或更深）
2. 确认所有目标段落的 paraId
3. 构造 batch JSON，一次性提交所有修改
4. 验证结果

**修改前必做检查清单：**
- [ ] 跑了 `officecli query image` 确认图片位置了吗？
- [ ] 目标段落是否包含图片？（含图片的段落绝对不能碰）
- [ ] 是否用了 stable paraId 寻址（而非位置索引）？

### 1.6 跨组件联动：Signal Counter 模式

**场景：** 组件 A（如 TransmitSequence）执行完动画后，需要触发组件 B（如 AppShell）重新显示一个 UI（如 Signal 屏幕）。A 调用 context 的 `resetXxx()` 重置状态，但 B 的本地 state（如 `showSignal`）不受 context 控制。

**问题：** context 重置了数据，但 B 不知道"该重新显示了"。

**解决：Signal Counter 模式**

```
context 中加一个计数器字段（如 signalGeneration: 0）
  │
  ├─ A 调用 resetXxx() → signalGeneration++
  │
  └─ B 用 useEffect 监听 signalGeneration
       └─ 变化时 → setShowXxx(true) + 导航到目标路由
```

**要点：**
- 计数器只增不减，每次重置都 `prev.xxx + 1`
- B 在 effect 中用 `if (counter > lastCounter)` 判断是否需要响应
- 适用于：动画结束后的 UI 重显、多步骤流程的阶段切换、跨路由的状态同步

---

## 二、用户写作风格手册

> **本章内容根据 `writing_style_mode` 配置决定生效比例。**
>
> | 模式 | Spotlight 内置规则（本章） | 用户个人风格（learn 复盘总结） |
> |------|-------------------|----------------------------|
> | `blend`（默认） | 50% | 50% |
> | `learn_only` | 0%（本章规则不生效） | 100% |
>
> **用户个人风格的来源：** 从 `{learn_path}` 中所有复盘文件的"用户方法论观察"章节提取。每次任务开始前，先回顾 learn 中总结的用户写作偏好。在 `blend` 模式下，这些偏好与本章内置规则各占一半；在 `learn_only` 模式下，完全以 learn 中的用户风格为准，本章内容仅作为背景参考。

### 2.1 三项铁律（CNN项目修正版，最高优先级）

> 这三条是经过CNN手写数字识别项目修正后确定的最终版本。在 `blend` 模式下优先级最高；在 `learn_only` 模式下不生效。

**铁律一：禁用括号附带参数。**

- [N] 错误：`LeakyReLU(负斜率0.1)`、`Adam(lr=0.0008)`、`Conv2d(3, 32, 3, 1)`
- 正确：`LeakyReLU，负斜率0.1`、`Adam，学习率0.0008`、`Conv2d，输入3通道，输出32通道，3x3卷积核，步长1`
- **规则：** 所有技术名词（函数名、类名、参数配置）一律用"名称 + 逗号 + 参数说明"，不使用括号包裹。参数之间也用逗号自然衔接。

**铁律二：步骤里不做回顾总结。**

- [N] 错误：在实验步骤末尾写"回顾整个实验过程，我完整地走通了……其中有几个关键收获……"
- 正确：步骤写完就停。不写心得、不写感悟、不写复盘。
- **规则：** 实验步骤是"做了什么"，不是"学到了什么"。如果总结内容有必要存在，应放在结果分析里，且一句话带过。

**铁律三：结果分析只写一段。**

- [N] 错误：分场景逐一剖析——"训练阶段……"、"GUI测试……"、"外部照片……"、"噪声测试……"、"综合总结……"
- 正确：所有结果汇成一段，给出关键数字和一句话结论，点到即止。
- **规则：** 结果分析是简要总结，不是分场景报告。一段话讲完所有要点。

**铁律二与铁律三的共存示例：**

> 注意：步骤 3、4 结尾没有"回顾"、"心得"；结果分析只有一个段落，包含关键数字和一句话原因分析，不分场景逐一展开。

### 2.2 五维风格框架（语义分割项目提炼）

适用于风格模仿类任务。分析任何参考文本时，从这五个维度入手：

| 维度 | 内容 | 默认偏好 |
|------|------|-------------|
| **整体结构** | 章节如何组织？粒度如何？ | **粗粒度模块**：按功能归并，不搞"1.1、1.2、1.3"式穷尽罗列 |
| **段落内部模式** | 段落内信息如何流动？ | **"思路→截图→验证"三段节奏**：先说设计意图 → 放代码/截图 → 观察结论收尾 |
| **语言特征** | 用词、句式、语气 | **第一人称叙事**："我定义了……""这里做了……"，实验报告读起来像开发日志，不是API文档 |
| **核心区别** | 和目标文体的典型写法有何不同？ | **叙事感 > 教科书感**：技术细节带着理由出现，不单独罗列 |
| **一句话概括** | 整篇风格的本质 | **粗粒度模块 + 第一人称 + 三段节奏 + 口语化过渡 + 设计决策带理由** |

### 2.3 图片与桥接规范

- **图片前**：用简短引导语（"关键代码如下："、"效果如下："）
- **图片后**：用"可以看到……"做观察结论
- **图片保护**：模板中的图片位置、编号、尺寸都是已审核过的，不重新排版
- **占位符格式**：`[结果参数]`、`[图片N]`，统一用方括号，与正文括号区分

### 2.4 "先搭框架后填数据"模式

用户偏好的工作方式：
1. 先写好报告骨架（所有段落、图片位置）
2. 用占位符标记需要后续填充的内容（`[结果参数]`、`[图片x]`）
3. 跑完实验拿到实际数据后一次性替换

---

## 三、用户思维与决策模式

### 3.1 核心特征

**迭代精炼型思维者。** 用户不会一次说完所有需求。先给出核心框架，在执行过程中逐层补充和修正。这不是需求不明确，而是用户的工作方式——在看见初版之后才能更好地判断下一步。

**"即时应用"要求者。** 新建立的规则或方法论，用户期望立刻在当前场景中践行一次。不等下次。这形成了"定规则→立刻用→写复盘"的闭环。

**自己做模板的主人。** 用户保留自己手写的大作业作为风格参照——"我写过的就是我认可的标准"。在做任何风格相关的工作时，优先参考用户自己的作品而非通用模板。

**"可追溯性优先"者。** 用户要求版本记录（version.md）、项目记忆（memory/）、复盘文件（learn/）三层可追溯体系。每次大修改后不仅要改代码，还要更新版本日志、写复盘、存记忆。这不是额外负担，而是用户的核心工作流——"改了什么、为什么改、学到了什么"三者缺一不可。

**"元工作流"观察者。** 用户不仅复盘项目内容，也复盘 AI 的执行过程。AI 在执行中出现的 error（如 Edit 匹配失败、python3 找不到）不是"默默修好就行"，而是值得写入复盘的重要内容。用户把 AI 的执行过程本身也视作可以被优化的"工作流"。

**设计敏感型决策者。** 用户对视觉设计有高度敏感——会指出"太暗了""太窄了""效果没体现"。偏好系统性全局调整（如全站字号统一放大、opacity 批量 +20）而非逐文件微调。修改方向倾向于：① 更大（字号、间距、容器宽度）② 更亮（颜色、对比度）③ 更满（利用左右空白区域）。

**`[xxN]` 占位符系统使用者。** 用户用 `[xx1]`、`[xx2]` … `[xxN]` 格式标记所有待填写的个人数据（姓名、学历、学校、兴趣、联系方式、项目链接等）。这是一种清晰可追溯的内容管理方式——明确区分"已确认的结构"和"待填充的数据"。AI 在搭建项目骨架时，不应填写虚假个人数据，而应使用此占位符系统。

**"慢即是快"偏好。** 用户说"慢慢优化下"是在明确要求小步迭代——每次只做一小步改动，等用户反馈后再继续。这不是效率低，而是用户享受打磨过程的信号。AI 应主动控制每次改动范围，不要一口气全部改完。除非用户明确说"一起改完"或"批量处理"。

### 3.2 指令风格解读

| 用户说什么 | 实际含义 |
|-----------|---------|
| "缩短点，讲清楚原理即可" | 给出方向但不限定具体长度，AI自行判断砍多少 |
| "ok" | 确认通过，继续下一个修改点 |
| "里面图片不动" | 模板中的图片位置/编号/尺寸已经审核过，不要碰 |
| "实验步骤改成1.xxx，2.xxx这样" | 理解意图，自行决定实现方式 |
| "结果可以写为[结果参数]" | 用占位符，先搭框架后填数据 |
| "再往下点、稍微放大、透明度稍微加大" | 模糊描述 → AI 自主推断具体数值（+20px、+4px、opacity+0.15）。一次同时调整 2-3 个维度比逐维迭代更高效 |

### 3.3 决策节奏

- **简短指令 → 期望AI自己判断实现方式**：不解释"为什么"，只讲"做什么"
- **修改完成后不主动要求验证**：AI应主动验证，但不需长篇报告
- **所有修改在同一文档上累积**：不做备份/另存，直接改原文件
- **事毕必复盘**：每个项目或重要任务结束后写 learn 总结
- **微调时一次改 2-3 维**：用户给模糊描述时，不要每次只改一个参数让他们反复确认。位置+大小+透明度一起调，缩短迭代轮次
- **"慢慢"是节奏信号**：听到"慢慢"时，每次只做一步，等反馈。不批量推进

### 3.4 验收标准

用户认可的工作成果通常具备：
1. **结构清晰**：一眼能看到层次
2. **不过度**：不会做多余的事（如多写心得、多分段落）
3. **一致性**：和用户自己写的风格一致
4. **闭环**：规则立了就用，任务完了就复盘

---

## 四、错误 → 正确路径（已踩坑清单）

### 4.1 officecli 相关

| 错误 | 症状 | 正确做法 |
|------|------|---------|
| **batch JSON中ASCII双引号未转义** | officecli JSON解析失败：`'C' is invalid after a value` | 中文文本中的引用全部用方括号 `[xxx]` 替代ASCII双引号。设置 `OFFICECLI_BATCH_ALLOW_STDIN_REDIRECT=1` |
| **set 替换段落文本丢失格式** | 原段落多run结构（如部分加粗）被替换为单个run | 格式统一的段落用set；混合格式段落用 `--find`/`--replace` |
| **位置索引寻址不稳定** | 增删段落后其他元素索引偏移 | 用 stable paraId 寻址：`p[@paraId=00100054]`，不用 `p[3]` |
| **逐段set效率低且易出错** | 多个段落逐个set，中间出错难定位 | 用 batch JSON 一次性提交，用 paraId 做路径锚点 |
| **get 输出过大** | stdout截断 | 用 `--depth 1` 看概要，完整内容会自动持久化到临时文件 |
| **未先确认图片位置就开始编辑** | 误删或覆盖含图片的段落 | 编辑前必跑 `officecli query image` 确认图片paraId |

### 4.2 文件操作相关

| 错误 | 症状 | 正确做法 |
|------|------|---------|
| **Edit 精确匹配失败（中英混合）** | old_string 匹配不到 | 中英混合、Em-dash、混合缩进的修改，用 Write 全量覆写，不用 Edit。**不仅适用于代码文件，SKILL.md、CLAUDE.md 等指令/配置文件同样适用**——Read 显示的空白可能与文件原始字节不一致，导致 Edit 反复失败 |
| **Glob 返回相对路径直接拼绝对路径** | File not found | Glob结果要确认是相对于哪个目录的，用 `find` 命令或 Bash 确认实际路径 |

### 4.3 理解偏差相关

| 错误 | 症状 | 正确做法 |
|------|------|---------|
| **凭印象改写，不先读全貌** | 改写结果和原文结构脱节 | 文档改写前先用 `get --depth 3` 读全貌 |
| **风格模仿时跳过中间检查** | 改完发现方向不对，大面积返工 | 先总结风格特征 → 用户确认 → 再批量执行 |
| **做了用户没要求的多余工作** | 多写心得段落、多分场景分析 | 严格遵守三项铁律，步骤不写回顾、结果只写一段 |

### 4.4 Python/Windows 环境相关

| 错误 | 症状 | 正确做法 |
|------|------|----------|
| **Windows 下使用 python3 命令** | 返回 exit code 49，无输出 | Windows 的 Python 安装为 python.exe，永远用 python 命令，不用 python3 |
| **Python 脚本 print 含 Unicode 特殊字符** | UnicodeEncodeError: gbk codec can't encode character | ① 脚本开头加 sys.stdout.reconfigure(encoding='utf-8') ；② 或使用纯 ASCII 标记（OK/FAIL）代替 Unicode 符号 |
| **多个脚本顺序修改同一文件** | 后执行的脚本模式匹配失效（pattern not found） | ① 后跑的脚本基于最新文件状态设计匹配模式；② 全部脚本跑完后统一验证，而非每脚本单独验证；③ pattern not found 不一定是错误——如果前面的脚本已完成该修改，可忽略 |

### 4.5 React/Three.js + WebGL 相关

| 错误 | 症状 | 正确做法 |
|------|------|---------|
| **`useMemo` 内做副作用更新 shader uniform** | Canvas resize 后 shader 分辨率不更新，或画面比例错误 | `useMemo` 用于缓存计算值，不用于副作用。uniform 值更新用 `useEffect`，uniforms 对象用 `useMemo` 只创建一次（空 deps 数组），后续值变更全部通过 `useEffect` 写入 `materialRef.current.uniforms.xxx.value` |
| **3D 对象在 Canvas 中不可见** | 黑洞/模型渲染不出来，用户说"没效果" | 定量验证可见性：`相机 FOV × 相机距离 × 物体 scale` → 计算物体在屏幕上的实际投影大小。相机在 z=0.5, fov=60, 物体在 z=-2.0  →  可见半高 = (0.5+2.0) × tan(30°) = 1.44。物体 plane scale=4  →  可见区域仅覆盖 UV 的 0.32-0.68 范围。物体核心半径必须远小于可见半高才能在屏幕上出现 |
| **深色主题下 opacity 叠加导致文字不可见** | 用户说"太暗了"、"看不清" | 两步排查法：① 检查基础色值本身是否够亮（如 `#5a7fba` 在深色背景上 opacity=1 时的对比度）；② 检查叠加的 opacity 值是否过低（`/20`、`/30` 在深色背景上几乎不可见）。两步都要调，光改 opacity 不够 |
| **GLSL 着色器多参数同步调优** | 改了单个参数，效果仍不明显 | 着色器亮度涉及 7+ 个参数需要同步调整：星空密度阈值、星空亮度系数、光子环强度、爱因斯坦环强度、吸积盘透明度、星云透明度、暗角强度。改其中一个效果有限，需要系统性调高所有相关参数 |
| **Canvas 内 plane 被其他物体遮挡或裁剪** | 3D 内容时有时无 | shaderMaterial 加 `depthTest={false}` `depthWrite={false}`。fixed 定位的 Canvas 容器确保 z-index 低于内容层 |

---

## 五、工具使用最佳实践速查

### 5.1 officecli 核心命令

| 操作 | 命令模板 |
|------|---------|
| 读取段落结构 | `officecli get file.docx /body/.../scope --depth 3 --json` |
| 修改段落文本 | `officecli set file.docx '/body/.../p[@paraId=XXX]' --prop text="..."` |
| 删除段落 | `officecli remove file.docx '/body/.../p[@paraId=XXX]'` |
| 文本查找替换 | `officecli set file.docx /scope --find "旧文本" --replace "新文本"` |
| 新增段落（锚点后） | `officecli add file.docx /parent --type paragraph --after '/body/.../p[@paraId=XXX]' --prop text="..."` |
| 查询图片位置 | `officecli query image file.docx` |
| 批量修改 | 构造JSON数组，`officecli batch file.docx batch.json` |
| 设置中文字体 | `--prop font.ea="宋体"` |
| 设置西文字体 | `--prop font.latin="Times New Roman"` |
| 设置行距 | `--prop lineSpacing="1.5x"` |
| 设置首行缩进 | `--prop firstLineIndent="24pt"` |

### 5.2 通用工具选择原则

| 场景 | 推荐工具 | 原因 |
|------|---------|------|
| 新建文件 | Write | 比Edit更可靠，尤其是中英混合内容 |
| 精确修改片段 | Edit | 只改一处时用 |
| 中英混合/特殊字符修改 | Write（全量覆写） | Edit的old_string匹配容易失败 |
| 搜索文件 | Glob | 比 `find` 更快，结果带路径 |
| 搜索文件内容 | Grep | 比 `grep` 更精准，支持正则 |
| 确认文件实际路径 | Bash `find` | Glob返回的相对路径可能不准确 |

### 5.3 项目初始化模板

```
{claudework_path}\YYYY年-MM月-DD日-项目名称\
├── README.md（可选）
├── CLAUDE.md（由 Init skill 生成）
└── ...（项目文件）
```

### 5.4 3D/WebGL 开发速查

| 场景 | 方法 |
|------|------|
| 验证 3D 对象是否在视野内 | `可见半高 = (相机z - 物体z) × tan(FOV/2)`，对比物体 scale |
| shader 亮度不足 | 同步调整 7+ 参数：星空密度/亮度、光子环、爱因斯坦环、吸积盘、星云、暗角 |
| Canvas resize 后 shader 比例错 | 用 `useEffect` 监听 `size` 变化，更新 `uResolution` uniform |
| 防止 3D 物体被裁剪 | shaderMaterial 加 `depthTest={false}` `depthWrite={false}` |
| Canvas 纹理文字被 DOM 遮挡 | 纹理中文字位置需考虑 DOM 层（nav、panel）的 z-index 和高度。先算纹理 UV→屏幕像素映射，再加 DOM 高度作为偏移量。导航栏通常 54-64px，纹理 y≈90 可避开 |
| 动态纹理更新（时钟/状态文字） | `drawTexture()` + `setInterval` + `te.needsUpdate = true` + cleanup `clearInterval`。所有需要透镜弯曲的文字必须走纹理路径，DOM 元素不被 shader 处理 |

### 5.5 sed 批量替换 Tailwind 类名规则

**场景：** 需要全局统一调整 Tailwind 的 opacity（如 `text-signal-dim/20` → `/40`）、宽度（如 `max-w-lg` → `max-w-xl`）等类名。

**核心规则：从高数值往低数值替换，避免连锁覆盖。**

```
[N] 错误顺序（低→高）：
  /20→/40 先执行 → 文件中出现 /40
  /40→/60 后执行 → 把刚替换出的 /40 也改了！
  结果：原来的 /20 变成了 /60（跳了两级）

正确顺序（高→低）：
  /70→/90 先执行 → 文件中的 /70 变成 /90
  /60→/80 再执行 → 不会匹配到刚生成的 /90
  /50→/70 ...
  ...
  /20→/40 最后执行 → 不会和前面的任何步骤冲突
```

**命令模板：**
```bash
for file in $(grep -rl "old-class-prefix" --include="*.tsx" dir/); do
  sed -i 's/old\/70/old\/90/g' "$file"   # 最高值先
  sed -i 's/old\/60/old\/80/g' "$file"
  sed -i 's/old\/20/old\/40/g' "$file"   # 最低值最后
done
```

---

## 六、复盘与持续学习机制

> 如果 `{mode}` = `"lightweight"`，跳过本章。用户可随时说"初始化 Spotlight 工作流"来启用完整功能。

### 6.1 何时写复盘

- 项目完成时
- 达到重要里程碑时
- 犯了一个值得记录的错误并找到了正确方法时
- 用户明确要求复盘时
- 发现了一个新的用户思维模式或偏好时

### 6.2 复盘文件结构

文件路径：`{learn_path}\YYYY年-MM月-DD日-项目名称.md`

```markdown
# 项目名称 — 一句话描述

## 项目概述
（1-2行，这个项目是什么）

## 错误 → 修复
- **错误描述** — 症状 → 修复方法

## 优化点
- **优化描述** — 原来的方式 → 改进后的方式

## 用户方法论观察
（用户思维、决策模式、工作流偏好等新发现）

## 应保留的模式
（从一开始就做对了、应该重复使用的模式）

## 核心经验教训
（2-5条可立即行动的经验规则）
```

### 6.3 复盘质量要求

- **具体，不是笼统**：写"batch JSON中ASCII双引号导致解析失败"而非"JSON有问题要注意"
- **可操作**：每条教训都应该能直接指导下一次行动
- **有场景**：说明在什么情况下触发了这个问题
- **关联用户思维**：不只是技术教训，也要记录对用户工作方式的新理解

### 6.4 每次任务开始前

如果 `{mode}` = `"full"`：**必须检查** `{learn_path}` 目录，读取最近的复盘摘要（见 §6.5），确保过往教训应用到当前任务。
如果 `{mode}` = `"lightweight"`：`{learn_path}` 为空，跳过此步骤。但如有最近会话中的经验教训，仍应在当前任务中应用。

### 6.5 复盘文件大小管理（防臃肿）

> **复盘库不是黑洞。** 不加限制地往 learn 里写会导致每次任务前加载一个几百 KB 的文件，严重挤压上下文窗口。

**写入规则：**

| 规则 | 说明 |
|------|------|
| **每条教训 ≤ 50 字** | 够说清"症状→修复"即可。[N] "在处理 officecli 的 batch JSON 操作时发现如果中文字符串里包含 ASCII 双引号会导致解析失败" → "batch JSON 中文引号导致解析失败，用方括号替代" |
| **每个复盘文件 ≤ 20 条** | 当错误→修复 + 优化点 + 核心教训 合计超过 20 条时，触发压缩 |
| **压缩策略** | 超过 20 条 → 合并归纳为 ≤ 5 条核心原则，替换原文件。被替换的内容压缩为一段摘要附在文件末尾 |

**读取规则：**

```
任务开始前回顾 learn：
      │
      ▼
┌──────────────────────────────────────┐
│  只读取：                             │
│  ① 最近 3 个复盘文件的"核心经验教训"    │
│  ② 最近 5 个复盘文件的文件标题         │
│                                      │
│  不读取：                             │
│  [N] 超过 3 个月的旧文件全文            │
│  [N] 详细的"错误→修复"列表（只在相关时搜索）│
│  [N] "用户方法论观察"长文（在执行中参考）  │
└──────────────────────────────────────┘
```

**自动维护：** 每次写入新复盘后，检查 learn 目录下的 `.md` 文件总数。超过 30 个时，将最旧的 10 个归档为 `archive/` 子目录并生成一句摘要索引。

---

## 七、核心铁律速查

> 以下铁律在每次任务开始前应快速过一遍。

### 写作铁律
1. **不用括号包参数** — 用逗号自然衔接："Adam，学习率0.0008" 而非 "Adam(lr=0.0008)"
2. **步骤不写回顾** — 步骤写完就停，不写心得感悟
3. **结果只写一段** — 汇总结论，不逐条展开
4. **禁用 Emoji** — 任何项目代码、文档、UI 中不使用彩色表情符号（除非用户明确要求）。功能性 Unicode 符号（← → ×）可保留。

### 思维铁律
4. **从总体反推细节** — 先看整体目标，逐层往下拆，不从细节堆
5. **拆解深度跟用户走** — 用户看哪里拆哪里，不提前拆没问的部分
6. **分析完必列 TODO（默认 3 条，复杂可 4+）** — 按任务类型选模板（Bug/开发/调研/文档/审查）；4+ 必须有明确理由

### 工作流铁律
7. **快速通道优先** — ≤ 20 字或单文件修改 → 跳过全流程，直接执行（§1.0.0）
8. **做完初版主动呈现，留迭代空间**
9. **新规则立完立刻用，形成闭环**
10. **事毕必复盘，写 learn 总结**

### 工具铁律
11. **officecli batch JSON 中禁止ASCII双引号** — 用方括号替代
12. **编辑前必跑 `officecli query image`** — 确认图片位置再动手
13. **用 stable paraId 寻址，不用位置索引**
14. **中英混合内容修改用 Write 全量覆写，不用 Edit**

### 学习铁律
15. **每次任务前只读近 3 篇复盘摘要** — 不复盘全库，防臃肿（§6.5）
16. **学人不只是学代码** — 观察用户的决策节奏、修正模式、验收标准

---

## 八、技能联动与推荐

### 8.1 任务执行中：调用已安装的技能

当用户的任务涉及以下场景时，先检查对应技能是否已安装（`~/.claude/skills/<skill-name>/SKILL.md` 是否存在），如果存在则加载使用：

| 场景 | 联动技能 | 技能用途 |
|------|---------|---------|
| 创建/编辑 Word 文档（.docx） | `officecli` | Word 文档的 CLI 读写改查 |
| 创建 PPT/演示文稿 | `ppt-master` | 多格式源文件生成 SVG 幻灯片并导出 PPTX |
| 学术论文写作/润色 | `nature-writing`、`nature-polishing` | Nature 风格的论文起草、润色 |
| 项目初始化 | `init` | 自动生成项目的 CLAUDE.md |
| 代码审查 | `code-review` | diff 级别的正确性/简洁性审查 |
| 安全审查 | `security-review` | 变更的安全漏洞审查 |
| 品牌/视觉设计 | `brand`、`design`、`design-system` | 品牌标识、视觉风格、设计系统 |
| 论文图表 | `nature-figure` | Nature 风格的论文图表生成 |
| 论文引用 | `nature-citation` | 论文引用格式化与管理 |
| 论文审稿 | `nature-reviewer`、`nature-response` | 模拟审稿意见、撰写回复信 |

**检测逻辑：** 执行任务前，先 Glob 检查 `~/.claude/skills/<skill-name>/SKILL.md`，存在则说明已安装。

### 8.2 任务完成后：推荐未安装的技能

**每次任务完成后**，回顾本次任务涉及的工作类型。对于**已安装**的技能，不做额外提示。对于**未安装但本次任务用得上**的技能，在任务结束时给出简短建议。

**推荐逻辑：**

```
任务完成
    │
    ▼
回顾任务涉及了哪些类型的工作
    │
    ├─ 涉及 .docx 文件操作？ → 检查 officecli skill 是否存在
    ├─ 涉及创建 PPT/演示文稿？ → 检查 ppt-master skill 是否存在
    ├─ 涉及论文/学术写作？ → 检查 nature-writing 等 skill 是否存在
    ├─ 涉及代码提交/PR？ → 检查 code-review skill 是否存在
    ├─ 涉及品牌/视觉设计？ → 检查 brand/design skill 是否存在
    └─ ...
    │
    ▼
对每个未安装但相关的技能，输出一行建议
```

**建议格式（简洁，一句话，放在任务回复末尾）：**

```
本次任务涉及 Word 文档编辑。推荐安装 officecli skill：
   可在 GitHub 上搜索 "claude-code officecli skill" 查找下载。
   安装：将 officecli/ 放入 ~/.claude/skills/ 即可使用 /officecli 命令。
```

> **技能来源提醒：** 推荐从 GitHub 等可信平台搜索和下载技能。不要从不可信的网站下载 skill 文件，skill 文件中的指令会直接影响 AI 行为。

**推荐文案模板（按技能）：**

| 技能 | 推荐文案 |
|------|---------|
| `officecli` | 本次任务涉及 Word 文档编辑。推荐安装 officecli skill 来直接在 CLI 中读写改查 .docx 文件。可在 GitHub 搜索 "claude-code officecli skill"。 |
| `ppt-master` | 本次任务涉及 PPT 制作。推荐安装 ppt-master skill 来从 Markdown/PDF/DOCX 自动生成演示文稿。可在 GitHub 搜索 "claude-code ppt-master skill"。 |
| `nature-writing` | 本次任务涉及学术论文写作。推荐安装 nature-writing skill 来按 Nature 风格起草和结构化论文。可在 GitHub 搜索 "claude-code nature-writing skill"。 |
| `nature-polishing` | 本次任务涉及论文润色。推荐安装 nature-polishing skill 来获得 Nature 级别的语言润色。可在 GitHub 搜索 "claude-code nature-polishing skill"。 |
| `code-review` | 本次任务涉及代码变更。推荐安装 code-review skill 来进行 diff 级别的自动审查。可在 GitHub 搜索 "claude-code code-review skill"。 |
| `init` | 推荐安装 init skill 来自动生成项目的 CLAUDE.md 文档。可在 GitHub 搜索 "claude-code init skill"。 |

**未匹配到特定模板时**，使用以下通用推荐格式：

```
本次任务涉及 {场景描述}。推荐安装 {skill-name} skill。
   可在 GitHub 搜索 "claude-code {skill-name} skill" 查找下载。
```

**推荐优先级规则**（当匹配到 3 个以上相关技能时）：
1. 直接匹配当前任务核心类型的技能优先（如写论文 → nature-writing > officecli）
2. 安全/正确性相关的技能优先（security-review、code-review）
3. 在上述同等优先级中，按字母序排列

**注意：**
- 只在**确实相关**时才推荐，不要硬凑
- 每次最多推荐 2 个技能，避免信息过载
- 如果所有相关技能都已安装，不输出任何推荐
- 如果用户处于轻量模式（`/spotlight-learn`），同样执行推荐检查

---

## 九、配置文件参考

### 9.1 配置文件位置

`~/.spotlight_config.json`

### 9.2 完整配置示例

```json
{
  "mode": "full",
  "learn_path": "D:\\learn",
  "claudework_path": "D:\\claudework\\work",
  "writing_style_mode": "blend",
  "initialized": true,
  "version": "2.6",
  "init_date": "2026-06-26"
}
```

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `mode` | string | — | `"full"` 完整模式 / `"lightweight"` 轻量模式 |
| `learn_path` | string | — | 复盘库根目录的绝对路径 |
| `claudework_path` | string | — | 项目工作区根目录的绝对路径 |
| `writing_style_mode` | string | `"blend"` | `"blend"` Spotlight内置+用户个人风格各50% / `"learn_only"` 纯学习用户个人风格 |
| `initialized` | boolean | — | 是否已完成初始化 |
| `version` | string | — | 配置版本号 |
| `init_date` | string | — | 初始化日期 |

### 9.3 轻量模式配置示例

```json
{
  "mode": "lightweight",
  "writing_style_mode": "blend",
  "initialized": true,
  "version": "2.6"
}
```

### 9.4 重置配置

用户说"重置 Spotlight 配置"或"重新初始化 Spotlight"时，删除 `~/.spotlight_config.json` 并重新执行 0.2-0.4 的初始化流程。

---

> **最后更新：** 2026-06-28
> **基于项目：** 学习库搭建（06-24）、语义分割实验报告（06-24）、CNN手写数字识别大作业（06-26）、Observation个人网站（06-27）、个人网站面板与时钟（06-28）
> **版本：** v2.6 — 新增快速通道（§1.0.0）：短问题/单文件跳过全流程；复盘文件大小管理（§6.5）：防臃肿压缩与摘要读取；命令入口简化：`/spotlight` 为唯一入口，子命令退为内部路由
